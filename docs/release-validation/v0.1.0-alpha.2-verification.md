# `v0.1.0-alpha.2` Post-Release Verification

**Date:** 2026-08-27
**Repository:** `michaelegner/architecture-intelligence-platform`

**Purpose of this document:** verify that `v0.1.0-alpha.2` closes the two open items from
[`v0.1.0-alpha.1-verification.md`](v0.1.0-alpha.1-verification.md): (1) the release pipeline fires
exactly one `docker.yml` run per release, not two, and (2) the non-root container fix is verified
against the actual pulled, tagged GHCR artifact — not only a locally built image.

## Release provenance

```text
Git tag v0.1.0-alpha.2
    -> commit 65264a12a7fef5d64794d2f2f887289e40691d15 (main, 2026-08-27T12:36:47Z)

Exactly ONE GitHub Actions `docker.yml` run fired for this release:
    -> run 33073289291 (release: published event) -> success, 58s

Digest pushed: sha256:cce60d4bf4185318c302b34c7aa3436c79340ef8254f1b5bea2b95b5cf3a7b91

ghcr.io/.../architecture-intelligence-platform:v0.1.0-alpha.2 and :latest
    both resolve to -> sha256:cce60d4bf4185318c302b34c7aa3436c79340ef8254f1b5bea2b95b5cf3a7b91
    confirmed via `gh api /users/michaelegner/packages/container/architecture-intelligence-platform/versions`
    as the only tagged version — no second run, no ambiguity, no race.
```

This confirms the `docker.yml` trigger fix (commit `e3e424b`, dropping the redundant
`push: tags: "v*"` trigger and keeping only `release: published`) actually works under a real
release, not just on paper. `v0.1.0-alpha.1` fired two runs from the same release event; this one
fired one.

`ci.yml` also ran against the tag ref (`33073288988`) and completed **success**.

## Pull-and-run verification

Same rigor as `v0.1.0-alpha.1`: authenticated pull, then a fully unauthenticated pull from a clean
credential state.

```bash
docker pull ghcr.io/michaelegner/architecture-intelligence-platform:v0.1.0-alpha.2
```

Digest matched `sha256:cce60d4...` exactly.

**Confirmed genuinely public**: `docker logout ghcr.io`, deleted the local image, pulled again with
**zero credentials** — succeeded.

### Non-root fix — verified against the pulled GHCR artifact itself

This is the gap `v0.1.0-alpha.1` left open: its non-root fix was only ever tested against a locally
built image from a later commit, never against the tagged/pulled release artifact (that tag's
digest predates the fix — see `v0.1.0-alpha.1-verification.md`'s provenance section).

Spun up a fresh Neo4j container and the pulled `v0.1.0-alpha.2` app image against it, on an isolated
network, and checked directly:

```bash
docker exec alpha2-verify-app whoami   # -> app
docker exec alpha2-verify-app id       # -> uid=1000(app) gid=1000(app) groups=1000(app)
```

Confirmed non-root, from the actual artifact a user would pull.

### Health / functional smoke check

Against the same running container:

- `GET /health` -> `{"status":"ok"}`
- `GET /health/neo4j` -> `{"status":"ok"}`
- `POST /api/import` -> success

Cleaned up afterward: containers and network removed.

## Outcome

Both open items from `v0.1.0-alpha.1` are closed:

- [x] Release pipeline fires exactly one `docker.yml` run per release.
- [x] Non-root fix verified against the actual pulled, tagged GHCR image (not just a local build).

No new defects found. `v0.1.0-alpha.2`'s tag, commit, and GHCR digest are unambiguous and
provenance-clean from the start — no retrofitting needed for this one.
